# Dependabot configuration. Single uv workspace: one lockfile at the root
# covers the main package and the mock server.

version: 2

updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  - package-ecosystem: "uv"
    directory: "/"
    schedule:
      interval: "weekly"
